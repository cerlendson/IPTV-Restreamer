{
  "name": "iptv-restreamer",
  "version": "1.0.0",
  "description": "Self-hosted IPTV browser and shared FFmpeg HLS restreamer.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/iptv.js && node --check src/stream-manager.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "fast-xml-parser": "^5.2.5"
  },
  "devDependencies": {}
}
